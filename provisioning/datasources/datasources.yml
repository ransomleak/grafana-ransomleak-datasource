apiVersion: 1

datasources:
  - name: 'ransomleak'
    type: 'ransomleak-ransomleak-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '/resources'
    secureJsonData:
      apiKey: 'api-key'
